Preserve the existing line endings of any file you edit.

Do not normalize line endings unless explicitly requested.

When modifying a file, keep its current end-of-line style (`CRLF` or `LF`) unchanged.
